# Sessione — n8n Queue Reader has_task:true Scheduled Polling Validation (Task 0116)

**Data:** 2026-05-12
**Task:** 0116-n8n-queue-reader-has-task-true-scheduled-polling-validation
**Tipo:** n8n-runtime-validation (completato)
**Runner:** n8n automation (polling run) + Claude Code (chiusura docs)

## Obiettivo

Validare che il polling automatico del queue reader `TEST - GitHub list Alina task queue`
(pubblicato come `queue-reader-schedule-5min`, Schedule Trigger ogni 5 minuti, Europe/Berlin)
gestisca correttamente un task eleggibile (`has_task:true`), generando i file attesi in
`docs/tasks/processing/` e `docs/sessions/automation-*`, e che un run successivo salti
il task grazie allo skip su `processing/`.

## Stato iniziale

- Queue reader `queue-reader-schedule-5min` attivo con Schedule Trigger (task 0115).
- Task `0116-n8n-queue-reader-has-task-true-scheduled-polling-validation.md` pushato in
  `docs/tasks/queue/` (commit `90fc4fc`, 2026-05-12).
- Nessun file in `docs/tasks/processing/` né `docs/tasks/done/` per il task 0116 al momento del push.
- Nessun runner automatico attivo.

## Evidenza GitHub — polling has_task:true

Il queue reader ha eseguito un run automatico entro 5 minuti dal push del task 0116.

| Artefatto | Commit | Timestamp |
|-----------|--------|-----------|
| Sessione automation | `bbef5d7` | 2026-05-12 05:50:53 +0200 |
| Cursor prompt in processing | `cb75002` | 2026-05-12 05:50:54 +0200 |

Timestamp interno sessione automation (UTC): `2026-05-12T03:50:52.701Z`.

### Sessione automation generata

File: `docs/sessions/automation-0116-n8n-queue-reader-has-task-true-scheduled-polling-validation.md`

Campi verificati:
- `Status: Generated by n8n automation workflow` — OK.
- `Task name: 0116-n8n-queue-reader-has-task-true-scheduled-polling-validation.md` — OK.
- `Task path: docs/tasks/queue/0116-n8n-queue-reader-has-task-true-scheduled-polling-validation.md` — OK.
- `Task SHA: 8bbe5399a118ccf60c0453c35db1aa45da17e0ae` — coerente con il commit del task in queue.
- `Type: n8n-runtime-validation` — OK.
- `Deploy policy: no` — OK.
- `Cursor prompt path: docs/tasks/processing/0116-...-cursor-prompt.md` — OK.
- `Cursor has not been executed yet` — corretto: runner manuale/supervisionato, non automatico.

### Cursor prompt generato

File: `docs/tasks/processing/0116-n8n-queue-reader-has-task-true-scheduled-polling-validation-cursor-prompt.md`

Contenuto verificato:
- Header `@docs/` references standard (roadmap, ORCHESTRATOR_RULES, AI_RULES, ecc.) — OK.
- `AGENT MODE` — OK.
- `Task source: docs/tasks/queue/0116-...` — OK.
- `Type: n8n-runtime-validation` — OK.
- `Deploy policy: no` — OK.
- Objective, Requirements, Expected output — coerenti con il task originale — OK.
- `Mandatory constraints` incluse (no git add ., no deploy, no tag) — OK.

## Esito has_task:true

Il queue reader ha selezionato correttamente il task 0116 come eleggibile (`has_task:true`),
generato sessione automation e cursor prompt. Nessun errore, nessuna modifica app, nessun
deploy, nessun tag, nessun rollback, nessun runner automatico attivato.

Criterio 1 (selezione `has_task:true`): **OK**
Criterio 2 (file generati in processing e sessions): **OK**

## Esito anti-doppio-run su processing

Dopo la generazione del prompt in `docs/tasks/processing/`, il queue reader non ha creato
ulteriori commit per il task 0116 nei tick successivi. Il file
`docs/tasks/processing/0116-...-cursor-prompt.md` è stato creato una sola volta (commit `cb75002`);
nessuna modifica ulteriore da n8n su questo task.

Lo skip su `processing/` ha funzionato correttamente: i run successivi hanno restituito
`has_task:false` (silenzioso, nessuna scrittura GitHub) per il task 0116.

Criterio 3 (anti-doppio-run): **OK**

## Rischi residui

- Il cursor prompt generato da n8n attende esecuzione manuale (o futura CLI runner).
  Non è richiesto dall'obiettivo di questo task, che era solo la validazione del polling.
- Il task 0116 resta in `docs/tasks/queue/` (nessun delete — policy corrente: copy-only,
  nessuna cancellazione da queue). Lo skip è garantito da `processing/`.
- La presenza del done marker `docs/tasks/done/0116-...md` garantisce che eventuali
  future sessioni non riprocessino il task (doppio skip: processing + done).

## Conferma vincoli

- Nessuna modifica a `src/**`. **OK**
- Nessuna modifica a `gas-current/**`. **OK**
- Nessuna modifica a `.gas/**`. **OK**
- Nessuna modifica a `appsscript.json`. **OK**
- Nessuna modifica a `package.json`. **OK**
- Nessun deploy Apps Script. **OK**
- Nessun tag. **OK**
- Nessun rollback. **OK**
- Nessun runner automatico Claude Code CLI / Cursor CLI attivato. **OK**
- Nessun export JSON n8n. **OK**
- Nessuna credenziale, token o URL raw sensibile. **OK**
- App Alina V1.9.2 stabile e non toccata. **OK**

## Done criteria verificati

1. n8n ha processato il task con `has_task:true`. **OK**
2. I file generati da n8n sono presenti e coerenti. **OK**
3. Il secondo run non riprocessa il task (skip su `processing/`). **OK**
4. Done marker creato: `docs/tasks/done/0116-n8n-queue-reader-has-task-true-scheduled-polling-validation.md`. **OK**
5. `PROJECT_STATE.md` e `CHECKPOINT.md` aggiornati. **OK** (in questo commit)
6. Nessuna modifica app, deploy, tag, rollback o runner automatico. **OK**
